# TODO — Grovia Automations

> Bron van waarheid voor taken is **Notion** (projecten: Grovia-Coding + Automatisering-Grovia).
> Items met `(lokaal)` staan (nog) niet in Notion — zie onderaan.

## Next Up

- **Geboortedatum/club/team uitrollen (commit `c72cd69`)** `(lokaal)` — vier stappen, in deze volgorde: (1) drie kolommen `geboortedatum_kind`/`club`/`team` **achteraan** toevoegen in het Deelnemers-werkboek, met kopregel; (2) de vier bijgewerkte bestanden (`Woo.gs`, `Deelnemers.gs`, `Sheet.gs`, `Dagelijks.gs`) plakken in de Apps Script-editor; (3) `vulGeboortedatumClubTeamVoorBestaandeRijen` één keer draaien (Apps Script-editor → functie selecteren → Run) en de samenvatting in het Execution-log checken (aantal bijgewerkt + eventueel "Niet gevonden"); (4) de functie daarna weer uit `Dagelijks.gs` verwijderen, zelfde discipline als eerdere eenmalige backfills. Verwacht: club/team blijven leeg voor rijen ouder dan begin juni 2026 (checkoutveld bestond toen nog niet) — geen bug. Zie [2026-08-12-geboortedatum-club-team-design.md](superpowers/specs/2026-08-12-geboortedatum-club-team-design.md).
- **Ixly-statusfix uitrollen (2 commits, nog niet gepusht)** `(lokaal)` — `bbc8aaf` (state `finished` i.p.v. `completed`) en `7525405` (alle vier adviseur-tokens proberen i.p.v. de eerste — zie hieronder). **Alleen `git push origin main` nodig**: dit keer raakt de fix uitsluitend Azure Functions, geen `.gs`-bestanden (de eerdere Controleren-aanpak in `bc7027e` is teruggedraaid, zie volgende punt).
- **Root cause #2 gevonden en gefixt: adviseur-loterij** — de Ixly-organisatie heeft vier api_users (Max Rood, Berry Moolenaar, Jeffry Moolenaar, Ruben Mogge), elk met een eigen access_grant. Een `candidate_task` is alleen zichtbaar voor de adviseur die de kandidaat bezit; `haal_token()` pakte altijd de eerste uit de lijst, dus elke run zag een willekeurige deelverzameling en de rest leek stil "niet af". Verklaart ook waarom `bc7027e` (404 = verouderde referentie) een verkeerde aanname was — teruggedraaid met `2f9618d`. **Nu ontgrendeld:** Berry's `user_uuid` is bekend (`7206dd4a-28e2-424d-ae67-fa464634a720`) — het al langer openstaande "Berry als vaste adviseur"-item hieronder kan nu opgepakt worden.
- **Vier rijen in Deelnemers hadden `action_type_af = JA` zonder type** `(lokaal)` — Roan Brethouwer (1270), Jon Kunst (1303), Dominic de Groot (1305), Oscar Reus (1326). Ontstaan doordat Max Ixly-uitslagen met de hand overnam maar per ongeluk in `action_type_af` i.p.v. `ixly_af` zette (`koppelReacties` zet die drie velden altijd samen, dus de code kan dit niet gemaakt hebben). Gevolg was dubbel: ze kregen nooit een Action Type-reminder, én als ze de test alsnog hadden gedaan was hun uitslag stil weggegooid (`ActionType.gs:52` stopt bij een rij die al op af staat). Volgens de live Ixly-stand (2026-08-12, met alle vier tokens): **Roan en Dominic zijn wél af** (ixly_af moet JA met ixly_op 2026-08-11) — **Jon Kunst en Oscar Reus staan nog op `created`**, bij hen moet action_type_af terug naar NEE (leeg) zonder ixly_af te zetten. Max handelt dit handmatig af in de Sheet.
- **Root cause van de dode debug-mail gevonden: Vimexx-verzendlimiet, zelfde als de SMTP-fout van 2026-08-10** `(lokaal)` — beide debug-mails ("Grovia Tag Callback" en "Grovia Test Router", zie item hieronder) gebruiken kale `wp_mail()` zonder eigen afzender en vallen dus terug op het globale WordPress-adres `b.moolenaar@grovia.nl` — hetzelfde adres dat vóór de FunnelKit-fix (zie hieronder) élke Welkomstmail verstuurde. Bij elke order liepen tot drie mails via dat ene adres. Op 2026-08-10 kwam dit zichtbaar naar buiten via een expliciete SMTP-bounce ("noreply@grovia.nl has sent too many emails", ontvanger Channa009@hotmail.com = order 1249) in sessie **"SMTP fout: te veel e-mails verzonden"** (`local_6b824ce4-11ed-4803-92ef-c6b282642e62`). `grovia_mail_log()` checkt de returnwaarde van `wp_mail()` niet, dus onze debug-logs faalden stil vanaf 08-06 zonder ooit een fout te tonen. **Die sessie heeft de afzender van de FunnelKit-Welkomstmail al live gesplitst over drie mailboxen** (schagenunitedacademie@/kolpingacademie@/minimove@grovia.nl, Step 15/17/23) — dat helpt de klantmail, maar **niet** onze twee debug-logs: die blijven op `b.moolenaar@grovia.nl` staan en dus kwetsbaar voor dezelfde limiet. Overwegen: debug-mails ook een eigen afzender geven, of gewoon verwijderen nu ze toch als TIJDELIJK gemarkeerd staan en hun nut al twijfelachtig is gebleken.
- **Zelfde sessie liet ook code-wijzigingen in de Function Apps achter, nooit gecommit of gedeployed** `(lokaal)` — `grovia_shared/grovia_mail.py` (+ `ixly-aanmelding`/`grovia-herinnering` als aanroepers) kreeg per-academie-afzender-ondersteuning (`SCHOOL_DATA[...]["afzender_naam"/"afzender_email"]`), 110/110 tests groen, maar staat nog steeds ongecommit in de working copy (dezelfde 4 bestanden die aan het begin van de huidige sessie al opvielen). Dit is de Function Apps-tegenhanger van de al live gezette FunnelKit-fix — zonder deze deploy blijven de Ixly-uitnodiging (`ixly-aanmelding`) en de reminder (`grovia-herinnering`) vanaf ÉÉN adres versturen, nog steeds kwetsbaar voor dezelfde Vimexx-limiet als het volume weer oploopt. Beslissen: alsnog committen + deployen (mailboxen bestaan al), of bewust laten liggen tot er een keus is over SMTP-relay (zie het advies in die sessie om op termijn naar Mailgun/SendGrid/Brevo te gaan i.p.v. shared-hosting SMTP).
- **Order 1344 (Jack/daniellerommers@hotmail.com, 2026-08-11): geen kandidaat in Ixly, oorzaak nog open** `(lokaal)` — `GET /candidates/api_identifier/1344` geeft 404. De mailbox-diagnose (zie hierboven) leverde niks op: de debug-mails waren al dood vóór deze order binnenkwam. Azure App Insights evenmin: het component (`grovia-automations202604301611`) bewaarde bij onderzoek minder dan een dag historie. **Twee dingen die alleen met WP-admin/FunnelKit-toegang te checken zijn (Max):** (1) heeft deze order/contact de FunnelKit-tag `SUC32627_jack_1344` gekregen? Zo nee, is de trigger nooit afgegaan (WooCommerce-ordertrigger-probleem, geen codebug). (2) Heeft FunnelKit een eigen executie-/auditlog voor de automation — staat daar een reden (bijv. de assessment-guard `Assessment2627_jack` die al bezet was door een eerdere aanmelding van hetzelfde kind)?
- **MiniMove: aankopen + aanwezigheid in Sheets, laatste stap** `(lokaal)` — code staat klaar en wordt live getest (`MiniMove.gs` nieuw, `Config.gs`/`Sheet.gs`/`Dagelijks.gs`/`Woo.gs` uitgebreid, 97 tests groen). Onderweg drie bugs gevonden én gefixt tijdens het live opzetten: (1) `MiniMove.gs` ontbrak als apart bestand in de Apps Script-editor, (2) `#ERROR!` op de `gebruikt`-formule door het verkeerde argument-scheidingsteken voor de Nederlandse werkboek-locale (nu `SpreadsheetApp.getSpreadsheetLocale()`), (3) nieuwe kindrijen aten de lege bufferregels vóór de volgende cyclusmarkering op (nu: nieuwe rij komt na de laatst gevulde rij, niet vlak vóór de marker). **Nog open, bij Max:** kolom O (rijen 1-4) in het Config-tabblad invullen met de cyclusnummers 1/2/3/4 naast de al ingevulde 8 trainingsdata per rij (P:W) — zonder die cyclusnummers blijft `config.minimove_kalender` leeg en komen de datums niet in de kolomkoppen van "MiniMove Aanwezigheid". Zie ADR-012.
- **WooCommerce REST API 403 (WAF) bij Vimexx** `(lokaal)` — terugkerend probleem (CONVENTIONS-regel 2), dit keer geraakt in Stap 6/7 van de dagelijkse run. Verzacht in `Woo.gs`: retry-met-backoff op 403, herkenbare User-Agent, pauzes tussen aanroepen/pagina's. Voor een permanente oplossing is een supportticket aan Vimexx opgesteld (vraagt om de WAF-regel te whitelisten of de toegestane requestfrequentie door te geven) — **nog niet verstuurd**: Max moet het exacte tijdstip van de laatste 403 (Log-tabblad of Apps Script-uitvoeringsgeschiedenis) en zijn klant-/pakketgegevens invullen.
- **Plugin v1.7 uploaden naar WordPress** `(lokaal)` — `grovia-automations.php` heeft de nieuwe fasecodes voor de 12 MiniMove-strippenkaart-slugs (+ de eerder ontbrekende `cyclus-4`); zolang niet geüpload blijft de debug-log bij een MiniMove-order "school of fase niet gevonden" zeggen in plaats van "MiniMove doet niet mee". Geen functionele impact (MM-check vangt het toch af), puur log-hygiëne.
- **`Financieel`-rapport gebruikt in juni/juli het verkeerde seizoen** — [Dagelijks.gs:168](../google-apps-script/deelnemers/Dagelijks.gs:168) voedt `berekenFinancieel()` met `bepaalSeizoen(vandaag)` (1-augustusgrens), terwijl het rapport de 1-junigrens aanhoudt. Op 2026-06-15 staat het rapport dus op seizoen 2526 én vallen de nieuwe juniorders buiten het venster — precies de orders waarvoor ADR-009 die grens bedacht. Nu niet actief (augustus), bijt weer in juni 2027. Fix (`bepaalFinancieelSeizoen`, vier regels + tests) staat uitgewerkt als Bijlage A + Task 9 van het strippenkaartplan.
- **Eerste automatische reminder-run controleren (vanaf 2026-08-05, 07:00)** `(lokaal)` — de trigger staat sinds 2026-08-04 aan. Verwacht patroon in het Log-tabblad: **2026-08-06** de nieuwe 2627-rijen (drempel 3 vanaf hun uitnodiging), **2026-08-07** de backlog (drempel 7 vanaf anker 2026-07-31). Gaat het eerder of massaler, dan is `reminder_anker` niet goed doorgekomen — dan direct de trigger pauzeren.
- **`order_ids`-Nederlandse-getalnotatie-bug fixen** — root cause gevonden: [Sheet.gs:92](../google-apps-script/deelnemers/Sheet.gs:92) schrijft de array weg met `waarde.join(',')` als onopgemaakte celwaarde, dus wordt `"935,1147"` door Sheets met een Nederlandse locale als getal geïnterpreteerd; bij teruglezen splitst [Sheet.gs:56](../google-apps-script/deelnemers/Sheet.gs:56) op komma en houdt één id over in plaats van twee. Fix: kolomformaat op tekst (`@`) zetten bij het schrijven. Nog open: of de al beschadigde `freddie-rood`-rij te repareren is of dat die order-id's handmatig terug moeten.
- **Uitnodigingsmail-template naar één startknop** — de twee aparte gameknoppen zijn overbodig (besloten 2026-08-04); één "Start hier"-knop met uitleg dat beide games daarna vanuit de Ixly-omgeving zelf te starten zijn. Wijziging in `grovia_mail.py` + deploy.
- **Berry als vaste adviseur op Ixly-kandidaten** — [ixly-aanmelding/__init__.py:156](../ixly-aanmelding/__init__.py:156) stuurt bij het aanmaken van een candidate geen `user_uuid` mee, waardoor Ixly zelf een adviseur toewijst (lijkt willekeurig, bevestigd 2026-08-12: kandidaten liggen verspreid over Max Rood/Berry/Jeffry/Ruben Mogge — zie het adviseur-loterij-item hierboven). Fix: `user_uuid` uit een env var meesturen. **Niet langer geblokkeerd:** Berry's `user_uuid` is bekend — `7206dd4a-28e2-424d-ae67-fa464634a720` (gevonden via `managed_organizations/{uuid}`'s `included[].type == 'api_user'`, `attributes.name`). Bij implementatie: de env var óók in `deploy.yml` zetten, anders komt de secret nooit in Azure aan. Lost de loterij niet met terugwerkende kracht op voor bestaande kandidaten (die blijven bij hun huidige adviseur) — de tokenfix hierboven blijft dus ook daarna nodig.
- **WhatsApp Business-account kan niet aan de groep worden toegevoegd** `(lokaal)` — géén codeprobleem: onze code verstuurt alleen een bericht met een groepsuitnodigingslink en bepaalt niets over wie mag joinen. Dit is gedrag van WhatsApp zelf of een groepsinstelling ("wie kan deelnemen"). Uitzetten bij Berry / de groepsbeheerder.
- **Overwegen: afzenderadres/-naam van de mail aanpassen** `(lokaal)` — staat nu op `noreply@grovia.nl` (kaal adres, geen weergavenaam). Voor bewust geparkeerd. Een ander adres vereist een nieuwe mailbox in DirectAdmin + drie GitHub Secrets (`SMTP_GEBRUIKER`/`SMTP_WACHTWOORD`/`SMTP_AFZENDER`) + deploy, want `SMTP_AFZENDER` is óók de envelope-sender. Alleen een nettere weergavenaam (`Grovia <noreply@grovia.nl>`) is één regel in `grovia_mail.py`.

1. **Action Type test-mail conditioneel versturen** — uitnodigingsmail moet NIET naar iedereen; voorwaarde-logica toevoegen aan de Grovia PHP-code (tag-logica) zodat alleen de juiste klanten de mail krijgen. Forms + sheets + scoring + mailtemplates zijn klaar (zie [ACTION-TYPE-TEST.md](ACTION-TYPE-TEST.md))
2. **FunnelKit automation inrichten** `(lokaal)` — één automation met decision tree, trigger op `WA_KA_VT`, `WA_KA_KT`, `WA_SU_VT`, `WA_SU_KT`, `WA_MM_VT`; per branch: remove trigger tag → conditie geen WAGroep-tag → HTTP Request Azure Function → add WAGroep-tag
3. **Database opzetten voor opslag testgegevens (Ixly brondata)** — Notion, prioriteit High
4. **Fysio-toestemming afronden** `(lokaal)` — (1) **WP-pagina `/toestemming-fysieke-intakes/` publiceren** met de inhoud uit [infopagina.html](../plugins/grovia-fysio-toestemming/infopagina.html) (links staan al live en geven nu 404), (2) **plugin v1.1.0 uploaden naar WordPress** — de vinkje-tekst is gelijkgetrokken met de toestemmingsverklaring; zolang dit niet gedeployed is staat de oude formulering nog live, (3) testorder met 100%-kortingscode om order-meta in admin te verifiëren (daarna order + code verwijderen). ~~(4) categorie `toestemming-vereist` aan overige trainingen hangen~~ — **gedaan, door de klant zelf**: staat inmiddels op vier producten (Zomerspektakel, Keeperstraining Kolping, Voetbaltraining Kolping én Schagen United), geverifieerd 2026-08-05 in wp-admin.
5. **Adres van SMC Dijk en Waard verifiëren** `(lokaal)` — de verklaring vermeldt "Helena Nordheimland 3, 1705 LM Heerhugowaard"; die straatnaam ziet er uit als een typo (Nordheimlaan?). Staat nu letterlijk zo op de infopagina — checken bij Berry/SMC voor publicatie, een fout adres op een live pagina is vervelend

## Later — Datawarehouse & teamindeling (Notion)

- [ ] Database inrichting
- [ ] Azure Portal
- [ ] PowerBI Licenties
- [ ] Geautomatiseerde teamindeling opzetten `(geblokkeerd: wacht op de eerste analyseerbare Ixly-respons — zonder echte scoredata is er niets om op in te delen)`
- [ ] Ixly score-response verifiëren voor Blocks Game en Rally Game via `explore.py` — voorwaarde voor de teamindeling hierboven
- [ ] Automatische teamranking opzetten (kids op volgorde van score, beste → minst goed) o.b.v. Ixly-data
- [ ] Google Sheet per vereniging aanmaken met vier tabbladen: jong voetbal, oud voetbal, jong keeper, oud keeper
- [ ] Script/trigger bouwen op Google Sheet die bij nieuwe rij Rubens-formule uitvoert en resultaat opslaat
- [ ] Google Sheet inrichten met tabblad ruwe antwoorden én tabblad met naam, ID, testuitslag en gekoppeld spelerprofiel
- [ ] Google Form aanmaken op basis van vragen/antwoorden persoonlijkheidstest
- [ ] E-mailreminder loop opzetten voor openstaande testen, met cap ~2–3 maanden
- [ ] Canva-koppeling: dynamische informatie automatisch vullen
- [ ] Voorstel maken omtrent automatisering

## Done

- [x] MiniMove-strippenkaarten volledig live in WooCommerce — 12 combinaties (cyclus × 4/6/8 keer) als samengestelde `pa_inschrijving`-waarden, uitklapbare checkout-weergave per cyclus, oude opties (Cyclus 1-4 los, Seizoenkaart, proeftrainingen) verwijderd als koopoptie, productbeschrijving bijgewerkt, maatvelden (shirt/broekje/sokken) zichtbaar bij een tenue/strippenkaart-aankoop maar bewust niet verplicht. Zie ADR-012 en [2026-08-05-minimove-strippenkaarten.md](superpowers/plans/2026-08-05-minimove-strippenkaarten.md) (2026-08-05, Max)
- [x] Fasecodes voor de 12 nieuwe MiniMove-slugs toegevoegd aan `$fase_map` in `grovia-automations.php` (log-hygiëne; MiniMove blijft functioneel uitgesloten via de bestaande MM-check) — plugin v1.6 → 1.7 (2026-08-05, Max)
- [x] Alle tijdelijke eenmalige functies uit `Dagelijks.gs` verwijderd — `backfillOudereOrders`, `backfillDiagnose`, `herstelVerlorenReminderVan20260803`, `vulRolProductBedragVoorBestaandeRijen`, `zetOudeRijenOpNieuwSchema`, `debugInschrijvingMeta`. Bestand van 536 naar 206 regels; op te halen uit de git-historie als er ooit één terug moet (2026-08-04, Max)
- [x] `backfillDiagnose()` gesloten zonder te draaien — de "120 orders → 0 nieuwe rijen"-uitkomst wordt niet verder onderzocht; bewuste keuze van Max, niet omdat het verklaard is (2026-08-04, Max)
- [x] Legacy-kandidaten (~30) eenmalig uitgenodigd voor de games via Ixly (2026-08-04, Max)
- [x] Kort klantbericht naar Grovia gestuurd over de eenmalige legacy-uitnodiging (2026-08-04, Max)
- [x] Toestemmingsverklaring (Grovia + SMC Dijk en Waard) verwerkt tot publicatieklare infopagina en de vinkje-tekst op de checkout gelijkgetrokken met de letterlijk voorgeschreven formulering uit die verklaring — plugin v1.1.0. Publiceren in WP en uploaden van de plugin staat nog open, zie Next Up #4 (2026-08-04, Max)
- [x] Klantvragen fysio-toestemming beantwoord — documentinhoud, gegevensdeling en pop-uptekst via de toestemmingsverklaring; intrekken gaat via `b.moolenaar@grovia.nl` met als gevolg geen deelname aan de volgende testronde voor zover het blessurepreventie betreft; SMC heeft een eigen privacyverklaring waar de infopagina nu naar linkt (2026-08-04, Max)
- [x] Automatische reminders live gezet — `installeerTrigger` gedraaid (dagelijks 07:00), ritme naar 3/7/14/21/35/49, backlog via de nieuwe `reminder_anker`-kolom op anker 2026-07-31 + teller 1 gezet zodat de handmatige reminder van 08-03 als #1 geldt. Zie ADR-010 (2026-08-04, Max)
- [x] Financieel-tabblad gebouwd en werkend — afdracht per vereniging × cyclus, keepers/spelers apart via cyclusproduct dan wel seizoenkaart, omzet incl./excl. 9% btw, €20 per deelnemer. Rekent op orderregelniveau met eigen seizoensgrens 1 juni; cyclus komt uit `pa_inschrijving` via `mapping.fases`. Ververst automatisch mee als Stap 6 van de dagelijkse run. Zie ADR-009 (2026-08-04, Max)
- [x] Kolommen `rol` (Speler/Keeper), `product` en `bedrag` toegevoegd aan het Deelnemers-tabblad, inclusief eenmalige backfill voor de bestaande rijen (2026-08-04, Max)
- [x] Race condition tussen de dagelijkse run en handmatige menu-acties gedicht met `LockService` — oorzaak van 27 reminders die wél in het Log stonden maar niet in de Deelnemers-sheet; verloren velden hersteld (2026-08-04, Max)
- [x] Oneindige-lus-bug in de reminderlogica gedicht — een rij met Action Type af, Ixly niet af én zonder `ixly_taken` kwam elke dag als kansloze mislukte poging terug (2026-08-04, Max)
- [x] Controleren of de dagelijkse Apps Script-trigger actief staat — gedraaid en bevestigd op 07:00 (2026-08-04, Max)
- [x] Apps Script gekoppeld aan een eigen GCP-project (`grovia-504418`) zodat externe testgebruikers (klant) het script mogen uitvoeren (2026-08-04, Max)
- [x] Action Type-controlecode-koppeling gefixt — `ACTION_TYPE_ENTRY_*`-env vars ontbraken volledig in `deploy.yml` (root cause van élke inzending in "Handmatig koppelen"); toegevoegd + geverifieerd in Azure. Kolomindex in `ActionType.gs` definitief bevestigd op 23 tegen de opgeschoonde KA/SU-antwoordsheets (2026-08-02, Max)
- [x] Ixly-passwordless-loginlink-mysterie afgesloten — geen bug: met een nieuw, nooit gebruikt testadres werkt de link gewoon; de eerdere "niet meer geldig"-melding kwam doordat het testadres al een bestaand Ixly-account had (2026-08-02, Max)

> 13 oudere Done-items getrimd op 2026-08-04; volledige historie staat in git.
