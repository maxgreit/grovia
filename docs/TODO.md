# TODO — Grovia Automations

> Bron van waarheid voor taken is **Notion** (projecten: Grovia-Coding + Automatisering-Grovia).
> Items met `(lokaal)` staan (nog) niet in Notion — zie onderaan.

## Next Up

- **Uitrol geboortedatum-behoud (ADR-016) afmaken** `(lokaal)` — stap 0 t/m 2 zijn op 2026-09-17 gedaan (code geplakt, "Alles nu verversen" groen, kolom D `yyyy-mm-dd`, team als `d-M`, Geboortedatums gevuld, indeling 63+51 regels, 21+8 zonder indeling). Nog open: stap 3, 4 en 5. Volledige lijst: (0) controleren dat de spreadsheet-tijdzone (Bestand → Instellingen) en de Apps Script-projecttijdzone allebei Europe/Amsterdam zijn, anders schuiven datums een dag; (1) Woo.gs, Sheet.gs, Teams.gs, Deelnemers.gs, Dagelijks.gs plakken in de Apps Script-editor; (2) "Alles nu verversen" draaien en controleren: kolom D toont `yyyy-mm-dd`, kolom F: team-waarden die een datum waren staan weer als `d-M`-tekst, verborgen tabblad "Geboortedatums" bestaat met ~98 rijen, runlog zonder "Geboortedatums MISLUKT", en vergelijk vóór en na de verversing drie bekende geboortedatums en drie team-waarden zoals `14-1` (kolom D en F); (3) tabblad "Overzicht" aanmaken met in A1 `=QUERY(Deelnemers!A:AA; "select * where B is not null"; 1)` en filterweergaven "Kolping" en "Schagen" (filter op `vereniging` = KA / SU); (4) Deelnemers en Geboortedatums beveiligen: Gegevens → Bladen en bereiken beveiligen → alleen Max; (5) bericht aan Berry en Jeffry: Deelnemers is alleen-lezen, filteren/sorteren in Overzicht via de filterweergaven, en de vraag wat zij op 7, 8 en 11 september deden.
- **`freddie-rood`-rij: `order_ids` staat corrupt (`935,935.9359351147`) — handmatig herstellen naar `935,1147` of de rij verwijderen (Max beslist); vanaf ADR-016 kan dit niet meer opnieuw ontstaan.**
- **7 commits pushen naar origin/main** `(lokaal)` — `4a0a77d..1a1f14f` staan alleen lokaal.
- **`migreerIxlyScoresSeizoen` verwijderen uit `Dagelijks.gs`** `(lokaal)` — de migratie is op 2026-08-26 gedraaid (repo én Apps Script-editor opschonen, zoals alle eenmalige functies).
- **31 kinderen in "Zonder indeling" nalopen na de ADR-015-uitrol** `(lokaal)` — KA 19 + SU 12 volgens de run van 2026-08-26; grotendeels de bekende gevallen (zes zonder totaalscore, drie zonder games, rest zonder geboortedatum), maar nog niet stuk voor stuk geverifieerd.
- **Batchverhonging bij de score-ophaling** `(lokaal)` — `kiesTeOphalenIndexen` selecteert in rijvolgorde zonder rotatie, anders dan `kiesTeControlerenIndexen` dat op `ixly_laatste_gecontroleerd_op` sorteert zodat elke rij een beurt krijgt. Een kind dat structureel geen volledige scores oplevert blijft nu permanent vooraan staan; bereikt het aantal zulke kinderen `ixly_batch_per_run` (standaard 50), dan krijgen nieuwe kinderen nooit meer een ophaalbeurt. De teller `zonderScores` in het runlog maakt het aantal zichtbaar, maar niet dat er kinderen achteraan verhongeren.

- **Antwoord van Ixly afwachten: is "Prestatie" een totaalscore van Blocks?** `(lokaal)` — Berry heeft de vraag op 2026-08-20 bij Ixly neergelegd. De API kent voor Blocks alleen `planning` en `flexibility`; de enige `performance` hoort bij Rally. Berry's formule staat intussen vast (Blocks-helft + Rally-helft, elk 50%) en draait met het Blocks-gemiddelde als Blocks-helft. Komt er een ander antwoord, dan is het alleen de Config-wegingen in `Y2:Z12` aanpassen — geen code, geen deploy.
- **Groepsvolgorde sterk → zwak: voorlopig akkoord** `(lokaal)` — Max houdt `C3`/`C2`/`C1` aan (sterkste bovenaan), zoals nu in Config `AE2:AE4` staat. Blijkt het omgekeerd te moeten, dan is het die drie cellen omdraaien. Niet langer blokkerend.
- **Teamindeling afmaken met Berry (morgen)** `(lokaal)` — de uitrol is grotendeels gedaan (gedeployed, Script Property gezet, tabbladen aangemaakt, Config gevuld, twee werkboeken, 16 kinderen automatisch ingedeeld). Wat nog moet: (1) de wegingen in Config `Y2:Z12` omzetten naar Berry's formule — `blocks_planning` en `blocks_flexibiliteit` op `1`, de vier Rally-indicatoren (`rally_consistentie`, `rally_volgehouden_aandacht`, `rally_respons_inhibitie`, `rally_reactie_op_fouten`) op `0,5`, de rest op `0`, wat (Blocks-gemiddelde + Rally-gemiddelde) ÷ 2 uitrekent; controleer dat de getallen rechts uitlijnen, links betekent tekst en telt niet mee; (2) `Teams.gs` opnieuw naar de Apps Script-editor voor het nieuwe tabblad "Teamindeling"; (3) "Alles nu verversen" draaien en controleren of de herberekende groepen logisch zijn; (4) het bericht over de "Prestatie"-kwestie en de zes benodigde cijfers naar Berry sturen.
- **Indeling controleren na de legacy-scores (2026-08-21)** `(lokaal)` — Max heeft het legacy-scoreblok in "Ixly Scores" geplakt en "Alles nu verversen" gedraaid. Controleren: staan de 21 complete kinderen verdeeld over de groepen, en precies de zes uitzonderingen + de drie zonder games in "Zonder indeling"? (Zie het Done-item en de Berry-notitie hieronder.)
- **Notitie voor Berry: zes kinderen zonder totaalscore** `(lokaal)` — uit de legacy-score-verzameling (2026-08-21). Vier kinderen missen `blocks_flexibiliteit` omdat hun Blocks-rapport letterlijk "onvoldoende informatie om op dit onderdeel een score te bepalen" zegt (ze losten de levels te vlot op): **nick-v-dalen, sven-breton, kiyo-van-de-geer, leon-gesko-caromelle**. Twee kinderen hebben alleen Blocks gedaan, Rally niet: **abdullah, stef-czapelski**. Alle zes belanden nu in "Zonder indeling" (een gewogen veld leeg = geen totaalscore, by design). Berry beslist: de vier flexibiliteit-gevallen een handmatige waarde geven of zo laten; de twee Rally-missers alsnog Rally laten spelen. Drie kinderen deden geen enkele game (duuk-van-houten, thijs-winder, delano-hewitt) — die blijven bewust leeg. Notitie staat klaar op `Desktop/Rapporten/NOTITIE-BERRY-zes-kinderen.md`.
- **Adviseurswijzigingen in Ixly eventueel terugdraaien** `(lokaal)` — om de rapporten te kunnen zien is bij ~22 legacy-kinderen de Ixly-adviseur van Berry/Ruben naar Max gezet. Als dat terug moet, is het handwerk per kind (bulk-adviseur bestaat niet in Ixly). Overleggen met Berry of dit nodig is.
- **Controle op onwaarschijnlijke geboortedata inbouwen** `(lokaal)` — nog niet gebouwd, drie keer aangeboden. In de brondata staan geboortedata van ouders (Yara Breton, 1984) en onmogelijke jaartallen (James Schultz, 2021 met team JO11-6). Zodra hun scores binnenkomen worden ze gewoon ingedeeld en is er op de trainerslijst niets aan te zien. Voorstel: een geboortejaar dat meer dan ~20 jaar terug ligt of in de toekomst valt gaat naar "Zonder indeling" met de reden "geboortedatum onwaarschijnlijk". Kleine wijziging in `bepaalLeeftijdsgroep`, met tests.
- **Testorder plaatsen en volledige keten verifiëren** `(lokaal)` — deze sessie zijn drie dingen tegelijk live gegaan (Ixly-statusfix, adviseur-loterij-fix, per-academie-afzender voor Ixly-mail) die nog niet met een echte order zijn bevestigd. Kolping of Schagen, Cyclus-product, 100%-kortingscode, eigen e-mailadres als besteller, checkoutvelden (Naam kind, Geboortedatum kind, Vereniging, Team) echt invullen. Checken: (1) uitnodigingsmail komt aan **vanaf `kolpingacademie@`/`schagenunitedacademie@grovia.nl`**, niet in spam; (2) `_grovia_ixly_taken` staat in de order-meta; (3) **bonus:** Blocks/Rally-games ook echt spelen om de `finished`-statusfix in de volgende `dagelijkseRun` mee te testen; (4) nieuwe rij in Deelnemers krijgt `geboortedatum_kind`/`club`/`team` meteen gevuld, op de juiste plek tussen naam_kind en vereniging. Komt de mail niet aan, dan bevestigt dat de Vimexx-verzendlimiet ook onze eigen Ixly-mail raakt, niet alleen FunnelKit. Order + code achteraf verwijderen.
- **Debug-mails ("Grovia Tag Callback"/"Grovia Test Router") blijven kwetsbaar voor de Vimexx-verzendlimiet** `(lokaal)` — root cause gevonden (zie Done): beide gebruiken kale `wp_mail()` zonder eigen afzender, dus `b.moolenaar@grovia.nl`. De per-academie-afzenderfix dekt dit niet (die geldt alleen voor de Welkomstmail/Ixly-uitnodiging). Beslissen: debug-mails ook een eigen afzender geven, of gewoon verwijderen nu ze als TIJDELIJK gemarkeerd staan en hun nut al twijfelachtig is gebleken.
- **Azure Application Insights toont geen historie ouder dan een paar uur, ondanks 90 dagen (component) / 30 dagen (workspace) ingestelde retentie** `(lokaal)` — gevonden tijdens het uitzoeken van de dode debug-mail. Geen dagcap ingesteld (`dailyQuotaGb: -1`), dus geen voor de hand liggende verklaring. Zonder fix is Azure-side loggen van Ixly-uitnodiging/reminder-mislukkingen straks even nutteloos als nu. Nader onderzoek nodig (support-ticket bij Microsoft, of check of de connection string pas recent goed staat).
- **Vier rijen in Deelnemers hadden `action_type_af = JA` zonder type** `(lokaal)` — Jon Kunst en Oscar Reus door Max met de hand teruggezet naar NEE (waren nog niet af volgens Ixly). Roan Brethouwer en Dominic de Groot waren wél echt af (`ixly_op` 2026-08-11) — die twee lost de eerstvolgende `dagelijkseRun` vanzelf op via de gedeployde state-fix, geen handmatige actie nodig. Afronden zodra die run bevestigd is gedraaid.
- **Order 1344 (Jack/daniellerommers@hotmail.com, 2026-08-11): geen kandidaat in Ixly, geparkeerd** `(lokaal)` — `GET /candidates/api_identifier/1344` geeft 404. De mailbox-diagnose (zie hierboven) leverde niks op: de debug-mails waren al dood vóór deze order binnenkwam. Azure App Insights evenmin: het component (`grovia-automations202604301611`) bewaarde bij onderzoek minder dan een dag historie. **Twee dingen die alleen met WP-admin/FunnelKit-toegang te checken zijn (Max):** (1) heeft deze order/contact de FunnelKit-tag `SUC32627_jack_1344` gekregen? Zo nee, is de trigger nooit afgegaan (WooCommerce-ordertrigger-probleem, geen codebug). (2) Heeft FunnelKit een eigen executie-/auditlog voor de automation — staat daar een reden (bijv. de assessment-guard `Assessment2627_jack` die al bezet was door een eerdere aanmelding van hetzelfde kind)?
- **MiniMove: aankopen + aanwezigheid in Sheets, laatste stap** `(lokaal)` — code staat klaar en wordt live getest (`MiniMove.gs` nieuw, `Config.gs`/`Sheet.gs`/`Dagelijks.gs`/`Woo.gs` uitgebreid, 97 tests groen). Onderweg drie bugs gevonden én gefixt tijdens het live opzetten: (1) `MiniMove.gs` ontbrak als apart bestand in de Apps Script-editor, (2) `#ERROR!` op de `gebruikt`-formule door het verkeerde argument-scheidingsteken voor de Nederlandse werkboek-locale (nu `SpreadsheetApp.getSpreadsheetLocale()`), (3) nieuwe kindrijen aten de lege bufferregels vóór de volgende cyclusmarkering op (nu: nieuwe rij komt na de laatst gevulde rij, niet vlak vóór de marker). **Nog open, bij Max:** kolom O (rijen 1-4) in het Config-tabblad invullen met de cyclusnummers 1/2/3/4 naast de al ingevulde 8 trainingsdata per rij (P:W) — zonder die cyclusnummers blijft `config.minimove_kalender` leeg en komen de datums niet in de kolomkoppen van "MiniMove Aanwezigheid". Zie ADR-012.
- **WooCommerce REST API 403 (WAF) bij Vimexx** `(lokaal)` — terugkerend probleem (CONVENTIONS-regel 2), dit keer geraakt in Stap 6/7 van de dagelijkse run. Verzacht in `Woo.gs`: retry-met-backoff op 403, herkenbare User-Agent, pauzes tussen aanroepen/pagina's. Voor een permanente oplossing is een supportticket aan Vimexx opgesteld (vraagt om de WAF-regel te whitelisten of de toegestane requestfrequentie door te geven) — **nog niet verstuurd**: Max moet het exacte tijdstip van de laatste 403 (Log-tabblad of Apps Script-uitvoeringsgeschiedenis) en zijn klant-/pakketgegevens invullen.
- **Plugin v1.7 uploaden naar WordPress** `(lokaal)` — `grovia-automations.php` heeft de nieuwe fasecodes voor de 12 MiniMove-strippenkaart-slugs (+ de eerder ontbrekende `cyclus-4`); zolang niet geüpload blijft de debug-log bij een MiniMove-order "school of fase niet gevonden" zeggen in plaats van "MiniMove doet niet mee". Geen functionele impact (MM-check vangt het toch af), puur log-hygiëne.
- **`Financieel`-rapport gebruikt in juni/juli het verkeerde seizoen** — [Dagelijks.gs:168](../google-apps-script/deelnemers/Dagelijks.gs:168) voedt `berekenFinancieel()` met `bepaalSeizoen(vandaag)` (1-augustusgrens), terwijl het rapport de 1-junigrens aanhoudt. Op 2026-06-15 staat het rapport dus op seizoen 2526 én vallen de nieuwe juniorders buiten het venster — precies de orders waarvoor ADR-009 die grens bedacht. Nu niet actief (augustus), bijt weer in juni 2027. Fix (`bepaalFinancieelSeizoen`, vier regels + tests) staat uitgewerkt als Bijlage A + Task 9 van het strippenkaartplan.
- **Eerste automatische reminder-run controleren (vanaf 2026-08-05, 07:00)** `(lokaal)` — de trigger staat sinds 2026-08-04 aan. Verwacht patroon in het Log-tabblad: **2026-08-06** de nieuwe 2627-rijen (drempel 3 vanaf hun uitnodiging), **2026-08-07** de backlog (drempel 7 vanaf anker 2026-07-31). Gaat het eerder of massaler, dan is `reminder_anker` niet goed doorgekomen — dan direct de trigger pauzeren.
- **Uitnodigingsmail-template naar één startknop** — de twee aparte gameknoppen zijn overbodig (besloten 2026-08-04); één "Start hier"-knop met uitleg dat beide games daarna vanuit de Ixly-omgeving zelf te starten zijn. Wijziging in `grovia_mail.py` + deploy.
- **Berry als vaste adviseur op Ixly-kandidaten** — [ixly-aanmelding/__init__.py:156](../ixly-aanmelding/__init__.py:156) stuurt bij het aanmaken van een candidate geen `user_uuid` mee, waardoor Ixly zelf een adviseur toewijst (lijkt willekeurig, bevestigd 2026-08-12: kandidaten liggen verspreid over Max Rood/Berry/Jeffry/Ruben Mogge, zie Done — de "adviseur-loterij"). Fix: `user_uuid` uit een env var meesturen. **Niet langer geblokkeerd:** Berry's `user_uuid` is bekend — `7206dd4a-28e2-424d-ae67-fa464634a720` (gevonden via `managed_organizations/{uuid}`'s `included[].type == 'api_user'`, `attributes.name`). Bij implementatie: de env var óók in `deploy.yml` zetten, anders komt de secret nooit in Azure aan. Lost de loterij niet met terugwerkende kracht op voor bestaande kandidaten (die blijven bij hun huidige adviseur) — de tokenfix (zie Done) blijft dus ook daarna nodig.
- **WhatsApp Business-account kan niet aan de groep worden toegevoegd** `(lokaal)` — géén codeprobleem: onze code verstuurt alleen een bericht met een groepsuitnodigingslink en bepaalt niets over wie mag joinen. Dit is gedrag van WhatsApp zelf of een groepsinstelling ("wie kan deelnemen"). Uitzetten bij Berry / de groepsbeheerder.

1. **Action Type test-mail conditioneel versturen** — uitnodigingsmail moet NIET naar iedereen; voorwaarde-logica toevoegen aan de Grovia PHP-code (tag-logica) zodat alleen de juiste klanten de mail krijgen. Forms + sheets + scoring + mailtemplates zijn klaar (zie [ACTION-TYPE-TEST.md](ACTION-TYPE-TEST.md))
2. **FunnelKit automation inrichten** `(lokaal)` — één automation met decision tree, trigger op `WA_KA_VT`, `WA_KA_KT`, `WA_SU_VT`, `WA_SU_KT`, `WA_MM_VT`; per branch: remove trigger tag → conditie geen WAGroep-tag → HTTP Request Azure Function → add WAGroep-tag
3. **Database opzetten voor opslag testgegevens (Ixly brondata)** — Notion, prioriteit High
4. **Fysio-toestemming afronden** `(lokaal)` — (1) **WP-pagina `/toestemming-fysieke-intakes/` publiceren** met de inhoud uit [infopagina.html](../plugins/grovia-fysio-toestemming/infopagina.html) (links staan al live en geven nu 404), (2) **plugin v1.1.0 uploaden naar WordPress** — de vinkje-tekst is gelijkgetrokken met de toestemmingsverklaring; zolang dit niet gedeployed is staat de oude formulering nog live, (3) testorder met 100%-kortingscode om order-meta in admin te verifiëren (daarna order + code verwijderen). ~~(4) categorie `toestemming-vereist` aan overige trainingen hangen~~ — **gedaan, door de klant zelf**: staat inmiddels op vier producten (Zomerspektakel, Keeperstraining Kolping, Voetbaltraining Kolping én Schagen United), geverifieerd 2026-08-05 in wp-admin.
5. **Adres van SMC Dijk en Waard verifiëren** `(lokaal)` — de verklaring vermeldt "Helena Nordheimland 3, 1705 LM Heerhugowaard"; die straatnaam ziet er uit als een typo (Nordheimlaan?). Staat nu letterlijk zo op de infopagina — checken bij Berry/SMC voor publicatie, een fout adres op een live pagina is vervelend

## Later — Datawarehouse & teamindeling (Notion)

- [ ] Database inrichting
- [ ] Azure Portal
- [ ] PowerBI Licenties
- ~~[ ] Geautomatiseerde teamindeling opzetten~~ — **gebouwd en uitgerold** (zie Done); het restwerk staat als losse items in Next Up en als Notion-taak "Teamindeling afronden en in gebruik nemen" (In progress). Zie [design-spec](superpowers/specs/2026-08-18-teamindeling-ixly-scores-design.md) en [implementatieplan](superpowers/plans/2026-08-18-teamindeling-ixly-scores.md).
- ~~[ ] Automatische teamranking opzetten (kids op volgorde van score, beste → minst goed) o.b.v. Ixly-data~~ — gedekt door de teamindeling hierboven (`Teams.gs` rangschikt op gewogen totaalscore, hoogste score = ranking 1); rolt mee uit bij de handmatige uitrol in Next Up
- ~~[ ] Google Sheet per vereniging aanmaken met vier tabbladen: jong voetbal, oud voetbal, jong keeper, oud keeper~~ — gedekt: wordt nu automatisch per vereniging aangemaakt, met een vijfde tabblad "Zonder indeling" erbij voor kinderen zonder volledige score of geboortedatum; het aanmaken en delen van de twee werkboeken zelf is nog een handmatige stap, zie Next Up
- [ ] E-mailreminder loop opzetten voor openstaande testen, met cap ~2–3 maanden
- [ ] Canva-koppeling: dynamische informatie automatisch vullen

## Done

- [x] `order_ids`-Nederlandse-getalnotatie-bug gefixt — root cause: [Sheet.gs:92](../google-apps-script/deelnemers/Sheet.gs:92) schreef de array weg met `waarde.join(',')` als onopgemaakte celwaarde, waardoor Sheets `"935,1147"` met een Nederlandse locale als getal interpreteerde en bij teruglezen ([Sheet.gs:56](../google-apps-script/deelnemers/Sheet.gs:56)) één id overhield in plaats van twee. Opgelost als onderdeel van ADR-016: kolomformaat op tekst (`@`) voor `order_ids` (en `geboortedatum_kind`/`team`) vóór `setValues` (`_forceerTekstKolommen`), ook in `schrijfMiniMoveDeelnemers` (tabblad "MiniMove Deelnemers" was nog onbeschermd) (2026-09-17, Max)
- [x] Dader van de geboortedatum-leegloop aangewezen — versiegeschiedenis, uitvoeringslogs en live code onderzocht: de live Apps Script-code is identiek aan de repo en heeft nooit een `WACHTER:`-regel gelogd, dus niet de run zelf. De datums verdwenen tussen runs door, tijdens handmatige rij-operaties van Berry en Jeffry (7 sep 16:47, 8 sep 09:24, 8 sep 20:54, 11 sep 08:16 — hele rijen gewijzigd t/m kolom AG); in dezelfde periode verdwenen ook datum-achtige `team`-waarden ("14-1") terwijl tekstwaarden bleven staan, dus het zijn datum-getypeerde cellen die sneuvelen bij die operaties. De exacte handeling is niet bekend — vraag open staand aan Berry/Jeffry. Fix (platte tekst + vangnet-tabblad) vastgelegd in ADR-016; Max heeft de datums op 2026-09-17 hersteld met `vulGeboortedatumClubTeamVoorBestaandeRijen` (2026-09-17, Max)
- [x] ADR-015 volledig uitgerold in het werkboek — kolommen `seizoen` (Ixly Scores) en `bedrag_correctie` (Deelnemers) ingevoegd, zeven .gs-bestanden geplakt, `migreerIxlyScoresSeizoen` gedraaid, "Alles nu verversen" groen: 8 stappen, 66 kinderen ingedeeld, 31 in "Zonder indeling". Erbij gebouwd en mee uitgerold: groepsnamenlijst per segment (`AG:AJ` kolom 4, bijv. `C3,C2a,C2b,C1`), totaalscore als derde kolom in "Teamindeling", leeftijdsgrens per academie (`AO2:AQ5`). Config-blokken AG:AJ en AO:AQ bewust leeg = standaardgedrag (2026-08-26, Max)
- [x] Seizoenstelling gecontroleerd bij de eerste run — de LET OP-regel meldde precies 1 deelnemer met seizoen 2526 buiten de 2627-indeling; plausibel, geen massale uitval, de 1-meigrens pakt goed uit (2026-08-26, Max)
- [x] Geboortedatum-leegloop in Deelnemers aangepakt — alle live .gs-schrijvers vergeleken met de repo (geen kan het legen verklaren), daarna fix in twee lagen gebouwd en gecommit (`183bfc7`): `erfGeboortedatums` (nieuwe seizoensrij erft de geboortedatum van een eerdere seizoensrij, draait elke run) en `beschermGeboortedatums`-wachter vóór elk van de vier schrijfmomenten (zet legen terug en meldt de schuldige stap in runlog + Log). `Deelnemers.gs` + `Dagelijks.gs` geplakt, datums opnieuw gevuld; node-tests 223 → 269 (2026-08-26, Max)
- [x] Legacy-testscores verzameld en in "Ixly Scores" gezet — route (a) via assignment-uuid's bleek onmogelijk (niet in de Ixly-UI, publieke API-lijst leeg), dus route (b): per kind adviseur op Max, rapport-PDF's gedownload, stenscores geparsed met `pdftotext`. 51 rapporten → **21 kinderen compleet**, plakblok over rij 35-65 van "Ixly Scores" (`bron` = `handmatig`, 27 slugs matchen exact met Deelnemers); Max geplakt en ververst. Zes uitzonderingen → notitie voor Berry (zie Next Up) (2026-08-21, Max)
- [x] Vier oude Action-Type-testinfra-items uit "Later" afgesloten via Notion-sync (stonden daar al op Done): Google Form persoonlijkheidstest, Google Sheet met ruwe antwoorden + uitslagen, script/trigger voor Rubens-formule, en het automatiseringsvoorstel (2026-08-21, Max)
- [x] Geautomatiseerde teamindeling gebouwd en uitgerold — nieuwe Azure Function `ixly-scores` (de zevende), `Scores.gs` en `Teams.gs`, tabblad "Ixly Scores", vier Config-blokken, stap 8 in de dagelijkse run, en per vereniging een werkboek met vier segmenttabbladen plus "Zonder indeling" en "Teamindeling". 30 commits, 115 nieuwe tests (node 108 → 223, pytest 118 → 135). 16 kinderen automatisch ingedeeld bij de eerste echte run. Zie [design-spec](superpowers/specs/2026-08-18-teamindeling-ixly-scores-design.md), [implementatieplan](superpowers/plans/2026-08-18-teamindeling-ixly-scores.md) en ADR-014 (2026-08-20, Max)
- [x] Ixly score-endpoint live geverifieerd — `GET /candidate_tasks/{uuid}/score` geeft negen genormeerde schalen (2 Blocks, 7 Rally) met `latent` op een 1-10-schaal; respons is cumulatief per kandidaat, niet per taak. Geverifieerd tegen Magnus Boekel (order 1345). Dit was het item dat de teamindeling blokkeerde (2026-08-20, Max)
- [x] `geboortedatum_kind` alsnog gevuld voor bestaande deelnemers — de backfill `vulGeboortedatumClubTeamVoorBestaandeRijen()` bleek nooit gedraaid te zijn; 167 van de 182 orders hadden die datum gewoon in de order-meta staan (2026-08-20, Max)
- [x] Seizoensgrens van de teamindeling vastgesteld op 1 mei — `bepaalTeamSeizoen()` leidt het seizoen af uit `uitgenodigd_op` en niet uit het opgeslagen `seizoen`-veld, dat met de 1-augustusregel op de orderdatum gestempeld is. Daarmee kent dit project bewust drie seizoensgrenzen; vastgelegd in GLOSSARY.md en ADR-014 (2026-08-20, Max)
- [x] Ixly score-response voor Blocks Game en Rally Game live geverifieerd tegen de echte API — `GET /candidate_tasks/{uuid}/score` werkt (HTTP 200). De respons bleek **cumulatief per kandidaat, niet per taak**: de Blocks-taak gaf alleen de blocks-node terug, de Rally-taak beide — de implementatie voegt de `normed`-dicts van alle taken samen in plaats van aan te nemen dat één aanroep alles heeft. Geverifieerd met de candidate_tasks van Magnus Boekel (order 1345, beide games `finished`). Basis voor de gebouwde geautomatiseerde teamindeling, zie Later (2026-08-18, Max)
- [x] Ixly-statusfix gedeployed: `state == 'finished'` i.p.v. het nooit-geverifieerde `'completed'` — de terugkoppeling had hierdoor nog nooit gewerkt. 116/97 tests groen, live geverifieerd tegen order 1246 (2026-08-12, Max)
- [x] Adviseur-loterij root cause gevonden en gefixt — Ixly-organisatie heeft vier api_users (Max Rood, Berry Moolenaar, Jeffry Moolenaar, Ruben Mogge); een `candidate_task` is alleen zichtbaar voor de adviseur die de kandidaat bezit, en `haal_token()` pakte altijd de eerste. Nu: alle vier tokens proberen tot de taak oplost. Verklaart ook waarom de eerdere "404 = verouderde referentie"-aanname (Controleren-aanpak) fout was — teruggedraaid. Gedeployed (2026-08-12, Max)
- [x] Root cause van de dode debug-mail ("Grovia Tag Callback"/"Grovia Test Router", stil sinds 2026-08-06) gevonden: dezelfde Vimexx-verzendlimiet als de SMTP-fout van 2026-08-10 (`noreply@grovia.nl`/`b.moolenaar@grovia.nl`, gedeelde mailbox tussen FunnelKit en de Azure Functions). Fix voor de klantmail (per-academie-afzender) hieronder; debug-mails zelf blijven kwetsbaar, zie Next Up (2026-08-12, Max)
- [x] Per-academie-afzender voor Ixly-uitnodiging (`ixly-aanmelding`) en reminder (`grovia-herinnering`) gecommit en gedeployed — Function Apps-tegenhanger van de eerder al live gezette FunnelKit-fix uit sessie "SMTP fout: te veel e-mails verzonden" (2026-08-12, Max)
- [x] Kolommen `geboortedatum_kind`/`club`/`team` toegevoegd aan Deelnemers (tussen naam_kind en vereniging), gevuld uit WooCommerce-checkoutvelden ('Geboortedatum kind'/'Vereniging'/'Team'), incl. eenmalige backfill voor bestaande rijen. `club` bewust niet 'vereniging' genoemd (die kolom bevat de academie-code KA/SU/MM). Zie [design-spec](superpowers/specs/2026-08-12-geboortedatum-club-team-design.md) (2026-08-12, Max)
- [x] Action Type-uitslagen gecontroleerd tegen Deelnemers: 36 van 37 kloppen. Vince van Cleef had een dubbele inzending (bevestigd: per ongeluk, geen actie nodig). Vier rijen bleken `action_type_af = JA` zonder type door een verkeerde handmatige invoer (moest `ixly_af` zijn) — Jon Kunst en Oscar Reus teruggezet naar NEE; Roan Brethouwer/Dominic de Groot waren écht af, lossen zichzelf op via de gedeployde statusfix (2026-08-12, Max)
> 10 oudere Done-items getrimd op 2026-08-26 (MiniMove-strippenkaarten, fasecodes v1.7, Dagelijks.gs-opschoning, legacy-uitnodiging, toestemmingsverklaring, reminders-livegang, Financieel-tabblad, e.a.); daarvoor 7 getrimd op 2026-08-12 (rol/product/bedrag-kolommen, LockService-race, reminder-oneindige-lus, trigger-check, GCP-koppeling, Action Type-koppeling, Ixly-passwordless-mysterie); volledige historie staat in git.
